// import { Selector } from 'testcafe'

// const appName = require('../package.json').description.toLowerCase()
// const pageURL = process.env.BASE_URL ? `${appName}${process.env.BASE_URL}` : `http://wmln2dapd0009/${appName}`

// fixture`Amp #1: Home page / Create Campaign`.page`${pageURL}`

// test('Page loads', async t => {
// })
